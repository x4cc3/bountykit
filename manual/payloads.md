# Payload Arsenal

> Canonical payload library lives in `../tracks/payloads/SKILL.md`. This file exists for reference routing only.

Load `../tracks/payloads/SKILL.md` for all payloads organized by bug class:
XSS, SQLi, NoSQLi, SSRF, SSTI, XXE, Command Injection, Path Traversal, Open Redirect, JWT, Prototype Pollution, CSRF Bypass, GraphQL.

Each class includes: detection, WAF bypass, context escapes, exfiltration, and chaining patterns.
